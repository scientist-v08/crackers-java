server:
  port: 7898

spring:
  application:
    name: vinayakatraders
  datasource:
    url: jdbc:postgresql://localhost:5432/crackersv3?sslmode=disable
    username: postgres
    password: Avaali1$
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: update # Dev only. For prod use 'validate' + Flyway
    show-sql: true
    properties:
      hibernate:
        '[format_sql]': true
        dialect: org.hibernate.dialect.PostgreSQLDialect

jwt:
  secret: ${JWT_SECRET:Eeh4ISeKyi6XaT7MJWJymuc+qsAel5TDxyBaqfBxh/k=}
  expiration-ms: 86400000 # 24 hours

logging:
  level:
    '[org.springframework.security]': DEBUG
